const { default: axios } = require("axios")

function onesignal_api() {
    //TODO: endpoint (POST) https://onesignal.com/api/v1/notifications

    const option = {

    }

    axios.post(endpoint, option, err => {

    }).then(res => {
        
    })
}

module.exports = onesignal_api